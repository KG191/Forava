import Foundation

// NOTE: This file contains legacy/simple cultural context model definitions that
// conflict with the richer canonical implementations in `ForavaApp/Contexts/`.
// Disable compilation of these legacy types to avoid duplicate-type errors.

#if false

/*
 The legacy definitions were intentionally preserved here for reference but are
 disabled from compilation. If you need to restore them, please port callers to
 use the canonical `ForavaApp/Contexts/*` implementations and then remove the
 surrounding `#if false` block.

 (Legacy code omitted)

*/

#endif
