import SwiftUI

// Payment receive view temporarily disabled - functionality to be implemented later
struct PaymentReceiveView: View {
    var body: some View {
        VStack {
            Text("Payment functionality coming soon")
                .font(.title2)
                .foregroundColor(.gray)
            
            Text("This feature will be implemented in a future update")
                .font(.body)
                .foregroundColor(.secondary)
                .multilineTextAlignment(.center)
                .padding()
        }
        .padding()
    }
}

#Preview {
    PaymentReceiveView()
}